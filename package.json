{
  "name": "escape-the-grandmother",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "RE7-style room-escape horror game with real-time DDGI in Three.js (WebGL2)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "puppeteer-core": "^25.1.0",
    "vite": "^5.4.10"
  }
}
